-- =============================================
-- Quuua Hub | Modern MM2 Edition - Часть 1: UI
-- =============================================

local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local CoreGui = game:GetService("CoreGui")

local LocalPlayer = Players.LocalPlayer

-- Создаем ScreenGui
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "QuuuaHub_Modern"
ScreenGui.ResetOnSpawn = false

local success = pcall(function()
    ScreenGui.Parent = CoreGui
end)
if not success then
    ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")
end

-- ==================== ЗАГРУЗЧИК ====================
local BlurOverlay = Instance.new("Frame", ScreenGui)
BlurOverlay.Size = UDim2.new(2, 0, 2, 0)
BlurOverlay.Position = UDim2.new(-0.5, 0, -0.5, 0)
BlurOverlay.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
BlurOverlay.BackgroundTransparency = 0.4
BlurOverlay.BorderSizePixel = 0
BlurOverlay.ZIndex = 50

local LoaderFrame = Instance.new("Frame", BlurOverlay)
LoaderFrame.Size = UDim2.new(0, 340, 0, 170)
LoaderFrame.Position = UDim2.new(0.5, -170, 0.5, -85)
LoaderFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
LoaderFrame.BorderSizePixel = 0
LoaderFrame.ZIndex = 51
Instance.new("UICorner", LoaderFrame).CornerRadius = UDim.new(0, 14)

local LoaderStroke = Instance.new("UIStroke", LoaderFrame)
LoaderStroke.Color = Color3.fromRGB(240, 60, 60)
LoaderStroke.Thickness = 1.5

local LoaderTitle = Instance.new("TextLabel", LoaderFrame)
LoaderTitle.Size = UDim2.new(1, 0, 0, 45)
LoaderTitle.Position = UDim2.new(0, 0, 0, 15)
LoaderTitle.BackgroundTransparency = 1
LoaderTitle.Text = "⚡ Quuua-hub"
LoaderTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
LoaderTitle.TextSize = 22
LoaderTitle.Font = Enum.Font.GothamBold
LoaderTitle.ZIndex = 52

local LoaderStatus = Instance.new("TextLabel", LoaderFrame)
LoaderStatus.Size = UDim2.new(1, 0, 0, 20)
LoaderStatus.Position = UDim2.new(0, 0, 0, 60)
LoaderStatus.BackgroundTransparency = 1
LoaderStatus.Text = "Загрузка..."
LoaderStatus.TextColor3 = Color3.fromRGB(150, 150, 160)
LoaderStatus.TextSize = 13
LoaderStatus.Font = Enum.Font.GothamMedium
LoaderStatus.ZIndex = 52

local BarBg = Instance.new("Frame", LoaderFrame)
BarBg.Size = UDim2.new(0, 280, 0, 8)
BarBg.Position = UDim2.new(0.5, -140, 0, 115)
BarBg.BackgroundColor3 = Color3.fromRGB(25, 25, 32)
BarBg.BorderSizePixel = 0
BarBg.ZIndex = 52
Instance.new("UICorner", BarBg).CornerRadius = UDim.new(1, 0)

local BarFill = Instance.new("Frame", BarBg)
BarFill.Size = UDim2.new(0, 0, 1, 0)
BarFill.BackgroundColor3 = Color3.fromRGB(240, 60, 60)
BarFill.BorderSizePixel = 0
BarFill.ZIndex = 53
Instance.new("UICorner", BarFill).CornerRadius = UDim.new(1, 0)

task.spawn(function()
    LoaderStatus.Text = "Инициализация Quuua-hub..."
    TweenService:Create(BarFill, TweenInfo.new(0.5, Enum.EasingStyle.Quad), {Size = UDim2.new(0.4, 0, 1, 0)}):Play()
    task.wait(0.6)
    LoaderStatus.Text = "Загрузка элементов интерфейса..."
    TweenService:Create(BarFill, TweenInfo.new(0.6, Enum.EasingStyle.Quad), {Size = UDim2.new(0.8, 0, 1, 0)}):Play()
    task.wait(0.7)
    LoaderStatus.Text = "Готово!"
    TweenService:Create(BarFill, TweenInfo.new(0.3, Enum.EasingStyle.Quad), {Size = UDim2.new(1, 0, 1, 0)}):Play()
    task.wait(0.4)
    
    local info = TweenInfo.new(0.4, Enum.EasingStyle.Quad)
    TweenService:Create(LoaderFrame, info, {BackgroundTransparency = 1}):Play()
    TweenService:Create(BlurOverlay, info, {BackgroundTransparency = 1}):Play()
    for _, c in pairs(LoaderFrame:GetDescendants()) do
        if c:IsA("TextLabel") then TweenService:Create(c, info, {TextTransparency = 1}):Play()
        elseif c:IsA("Frame") then TweenService:Create(c, info, {BackgroundTransparency = 1}):Play() end
    end
    task.wait(0.4)
    BlurOverlay:Destroy()
end)

-- ==================== ОСНОВНОЕ ОКНО ====================
_G.CurrentAccent = Color3.fromRGB(240, 60, 60)
_G.isRGB = false
_G.InteractiveElements = {}

local MainFrame = Instance.new("Frame", ScreenGui)
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 520, 0, 340)
MainFrame.Position = UDim2.new(0.5, -260, 0.5, -170)
MainFrame.BackgroundColor3 = Color3.fromRGB(18, 18, 24)
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Draggable = true
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 12)

local MainStroke = Instance.new("UIStroke", MainFrame)
MainStroke.Color = _G.CurrentAccent
MainStroke.Thickness = 1.5
table.insert(_G.InteractiveElements, MainStroke)

local SizeConstraint = Instance.new("UISizeConstraint", MainFrame)
SizeConstraint.MinSize = Vector2.new(420, 280)
SizeConstraint.MaxSize = Vector2.new(850, 650)

-- Кнопка растягивания окна
local ResizeCorner = Instance.new("TextButton", MainFrame)
ResizeCorner.Size = UDim2.new(0, 30, 0, 30)
ResizeCorner.Position = UDim2.new(1, -30, 1, -30)
ResizeCorner.BackgroundTransparency = 1
ResizeCorner.Text = "◢"
ResizeCorner.TextColor3 = Color3.fromRGB(180, 180, 190)
ResizeCorner.TextSize = 16
ResizeCorner.ZIndex = 10

local resizing = false
ResizeCorner.MouseButton1Down:Connect(function() resizing = true end)
UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then resizing = false end
end)
UserInputService.InputChanged:Connect(function(input)
    if resizing and input.UserInputType == Enum.UserInputType.MouseMovement then
        local mousePos = UserInputService:GetMouseLocation()
        local newX = math.clamp(mousePos.X - MainFrame.AbsolutePosition.X, 420, 850)
        local newY = math.clamp(mousePos.Y - MainFrame.AbsolutePosition.Y, 280, 650)
        MainFrame.Size = UDim2.new(0, newX, 0, newY)
    end
end)

-- Шапка
local Header = Instance.new("Frame", MainFrame)
Header.Size = UDim2.new(1, 0, 0, 40)
Header.BackgroundColor3 = Color3.fromRGB(24, 24, 32)
Header.BorderSizePixel = 0
Instance.new("UICorner", Header).CornerRadius = UDim.new(0, 12)

local Title = Instance.new("TextLabel", Header)
Title.Size = UDim2.new(1, -90, 1, 0)
Title.Position = UDim2.new(0, 14, 0, 0)
Title.BackgroundTransparency = 1
Title.Text = "⚡ Quuua-hub | MM2"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 14
Title.Font = Enum.Font.GothamBold
Title.TextXAlignment = Enum.TextXAlignment.Left

-- Кнопки управления окном (Закрыть / Свернуть)
local CloseBtn = Instance.new("TextButton", Header)
CloseBtn.Size = UDim2.new(0, 26, 0, 26)
CloseBtn.Position = UDim2.new(1, -34, 0, 7)
CloseBtn.BackgroundColor3 = Color3.fromRGB(220, 50, 50)
CloseBtn.Text = "✕"
CloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseBtn.Font = Enum.Font.GothamBold
Instance.new("UICorner", CloseBtn).CornerRadius = UDim.new(0, 6)
Instance.new("UIStroke", CloseBtn).Color = Color3.fromRGB(255, 100, 100)

CloseBtn.MouseButton1Click:Connect(function() ScreenGui:Destroy() end)

local MiniBar = Instance.new("TextButton", ScreenGui)
MiniBar.Size = UDim2.new(0, 150, 0, 36)
MiniBar.Position = UDim2.new(0.5, -75, 0.05, 0)
MiniBar.BackgroundColor3 = _G.CurrentAccent
MiniBar.Text = "⚡ Quuua-hub"
MiniBar.TextColor3 = Color3.fromRGB(255, 255, 255)
MiniBar.TextSize = 13
MiniBar.Font = Enum.Font.GothamBold
MiniBar.Visible = false
MiniBar.Active = true
MiniBar.Draggable = true
Instance.new("UICorner", MiniBar).CornerRadius = UDim.new(0, 8)
table.insert(_G.InteractiveElements, MiniBar)

local MinimizeBtn = Instance.new("TextButton", Header)
MinimizeBtn.Size = UDim2.new(0, 26, 0, 26)
MinimizeBtn.Position = UDim2.new(1, -64, 0, 7)
MinimizeBtn.BackgroundColor3 = Color3.fromRGB(50, 50, 64)
MinimizeBtn.Text = "–"
MinimizeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
MinimizeBtn.Font = Enum.Font.GothamBold
Instance.new("UICorner", MinimizeBtn).CornerRadius = UDim.new(0, 6)

MinimizeBtn.MouseButton1Click:Connect(function()
    MainFrame.Visible = false
    MiniBar.Visible = true
end)

MiniBar.MouseButton1Click:Connect(function()
    MiniBar.Visible = false
    MainFrame.Visible = true
end)

-- Боковое меню и контейнер контента
local Sidebar = Instance.new("ScrollingFrame", MainFrame)
Sidebar.Size = UDim2.new(0, 135, 1, -48)
Sidebar.Position = UDim2.new(0, 6, 0, 44)
Sidebar.BackgroundTransparency = 1
Sidebar.ScrollBarThickness = 2

local SidebarLayout = Instance.new("UIListLayout", Sidebar)
SidebarLayout.Padding = UDim.new(0, 3)

local ContentArea = Instance.new("Frame", MainFrame)
ContentArea.Size = UDim2.new(1, -150, 1, -48)
ContentArea.Position = UDim2.new(0, 146, 0, 44)
ContentArea.BackgroundTransparency = 1

local activeTabContainer = nil

local function UpdateThemeColor(newColor)
    _G.CurrentAccent = newColor
    for _, el in ipairs(_G.InteractiveElements) do
        if el and el.Parent then
            if el:IsA("UIStroke") then
                el.Color = newColor
            else
                el.BackgroundColor3 = newColor
            end
        end
    end
end

task.spawn(function()
    local hue = 0
    while true do
        if _G.isRGB then
            hue = (hue + 0.005) % 1
            UpdateThemeColor(Color3.fromHSV(hue, 1, 1))
        end
        task.wait(0.03)
    end
end)

function CreateTab(name)
    local tabBtn = Instance.new("TextButton", Sidebar)
    tabBtn.Size = UDim2.new(1, -4, 0, 32)
    tabBtn.BackgroundTransparency = 1
    tabBtn.Text = "  " .. name
    tabBtn.TextColor3 = Color3.fromRGB(150, 150, 160)
    tabBtn.TextSize = 11
    tabBtn.Font = Enum.Font.GothamBold
    tabBtn.TextXAlignment = Enum.TextXAlignment.Left

    local container = Instance.new("ScrollingFrame", ContentArea)
    container.Size = UDim2.new(1, 0, 1, 0)
    container.BackgroundTransparency = 1
    container.ScrollBarThickness = 3
    container.Visible = false

    local list = Instance.new("UIListLayout", container)
    list.Padding = UDim.new(0, 5)
    list:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
        container.CanvasSize = UDim2.new(0, 0, 0, list.AbsoluteContentSize.Y + 10)
    end)

    tabBtn.MouseButton1Click:Connect(function()
        for _, c in pairs(ContentArea:GetChildren()) do if c:IsA("ScrollingFrame") then c.Visible = false end end
        for _, b in pairs(Sidebar:GetChildren()) do if b:IsA("TextButton") then b.TextColor3 = Color3.fromRGB(150, 150, 160) end end
        container.Visible = true
        tabBtn.TextColor3 = _G.CurrentAccent
    end)

    if not activeTabContainer then
        activeTabContainer = container
        container.Visible = true
        tabBtn.TextColor3 = _G.CurrentAccent
    end

    return container
end

function CreateToggle(parent, title, callback)
    local btn = Instance.new("TextButton", parent)
    btn.Size = UDim2.new(1, -6, 0, 34)
    btn.BackgroundColor3 = Color3.fromRGB(26, 26, 34)
    btn.Text = "  " .. title
    btn.TextColor3 = Color3.fromRGB(230, 230, 240)
    btn.TextSize = 11
    btn.Font = Enum.Font.GothamMedium
    btn.TextXAlignment = Enum.TextXAlignment.Left
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)

    local statusBox = Instance.new("TextLabel", btn)
    statusBox.Size = UDim2.new(0, 40, 0, 20)
    statusBox.Position = UDim2.new(1, -46, 0.5, -10)
    statusBox.BackgroundColor3 = Color3.fromRGB(20, 20, 26)
    statusBox.Text = "OFF"
    statusBox.TextColor3 = Color3.fromRGB(140, 140, 150)
    statusBox.TextSize = 10
    statusBox.Font = Enum.Font.GothamBold
    Instance.new("UICorner", statusBox).CornerRadius = UDim.new(0, 4)

    local state = false
    btn.MouseButton1Click:Connect(function()
        state = not state
        statusBox.Text = state and "ON" or "OFF"
        statusBox.TextColor3 = state and Color3.new(1,1,1) or Color3.fromRGB(140, 140, 150)
        statusBox.BackgroundColor3 = state and _G.CurrentAccent or Color3.fromRGB(20, 20, 26)
        if state then table.insert(_G.InteractiveElements, statusBox) end
        callback(state)
    end)
end
-- =============================================
-- Quuua Hub | Modern MM2 Edition - Часть 2: Функционал
-- =============================================

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")

local LocalPlayer = Players.LocalPlayer
local Camera = Workspace.CurrentCamera

-- ==================== 1. ОБЩЕЕ ====================
local TabGeneral = CreateTab("🎯 Общее")

CreateToggle(TabGeneral, "ESP (Мардер-Красный, Шериф-Синий)", function(v)
    _G.GeneralESP = v
    task.spawn(function()
        while _G.GeneralESP do
            for _, p in pairs(Players:GetPlayers()) do
                if p ~= LocalPlayer and p.Character then
                    local h = p.Character:FindFirstChild("QuuuaRoleESP")
                    if not h then
                        h = Instance.new("Highlight", p.Character)
                        h.Name = "QuuuaRoleESP"
                    end
                    local hasKnife = p.Backpack:FindFirstChild("Knife") or p.Character:FindFirstChild("Knife")
                    local hasGun = p.Backpack:FindFirstChild("Gun") or p.Character:FindFirstChild("Gun")
                    if hasKnife then
                        h.FillColor = Color3.fromRGB(240, 50, 50)
                    elseif hasGun then
                        h.FillColor = Color3.fromRGB(60, 150, 255)
                    else
                        h.FillColor = Color3.fromRGB(50, 215, 110)
                    end
                end
            end
            task.wait(0.5)
        end
        for _, p in pairs(Players:GetPlayers()) do
            if p.Character then
                local h = p.Character:FindFirstChild("QuuuaRoleESP")
                if h then h:Destroy() end
            end
        end
    end)
end)

CreateToggle(TabGeneral, "Aimbot (Общий)", function(v)
    _G.GeneralAimbot = v
    task.spawn(function()
        while _G.GeneralAimbot do
            pcall(function()
                local nearestPlayer = nil
                local shortestDist = math.huge
                local mousePos = game:GetService("UserInputService"):GetMouseLocation()
                
                for _, p in pairs(Players:GetPlayers()) do
                    if p ~= LocalPlayer and p.Character and p.Character:FindFirstChild("Head") then
                        local hum = p.Character:FindFirstChildOfClass("Humanoid")
                        if hum and hum.Health > 0 then
                            local screenPos, onScreen = Camera:WorldToViewportPoint(p.Character.Head.Position)
                            if onScreen then
                                local dist = (Vector2.new(screenPos.X, screenPos.Y) - mousePos).Magnitude
                                if dist < shortestDist then
                                    shortestDist = dist
                                    nearestPlayer = p
                                end
                            end
                        end
                    end
                end
                
                if nearestPlayer and nearestPlayer.Character and nearestPlayer.Character:FindFirstChild("Head") then
                    Camera.CFrame = CFrame.new(Camera.CFrame.Position, nearestPlayer.Character.Head.Position)
                end
            end)
            task.wait()
        end
    end)
end)

CreateToggle(TabGeneral, "Большие головы (Head Size)", function(v)
    _G.BigHeads = v
    task.spawn(function()
        while _G.BigHeads do
            for _, p in pairs(Players:GetPlayers()) do
                if p ~= LocalPlayer and p.Character and p.Character:FindFirstChild("Head") then
                    p.Character.Head.Size = Vector3.new(3, 3, 3)
                    p.Character.Head.CanCollide = false
                end
            end
            task.wait(1)
        end
    end)
end)

CreateToggle(TabGeneral, "Ноклип (Проход сквозь стены)", function(v)
    _G.NoclipActive = v
    RunService.Stepped:Connect(function()
        if _G.NoclipActive and LocalPlayer.Character then
            for _, part in pairs(LocalPlayer.Character:GetDescendants()) do
                if part:IsA("BasePart") then
                    part.CanCollide = false
                end
            end
        end
    end)
end)

-- ==================== 2. КИЛЛЕР ====================
local TabKiller = CreateTab("🔪 Киллер")

CreateToggle(TabKiller, "Убить всех сразу [Зациклено]", function(v)
    _G.KillAllActive = v
    if v then
        task.spawn(function()
            while _G.KillAllActive do
                pcall(function()
                    local char = LocalPlayer.Character
                    local rootPart = char and char:FindFirstChild("HumanoidRootPart")
                    local knife = char and char:FindFirstChild("Knife") or LocalPlayer.Backpack:FindFirstChild("Knife")
                    if rootPart and knife then
                        if knife.Parent == LocalPlayer.Backpack then knife.Parent = char end
                        for _, p in pairs(Players:GetPlayers()) do
                            if not _G.KillAllActive then break end
                            if p ~= LocalPlayer and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
                                local targetHumanoid = p.Character:FindFirstChildOfClass("Humanoid")
                                if targetHumanoid and targetHumanoid.Health > 0 then
                                    rootPart.CFrame = p.Character.HumanoidRootPart.CFrame * CFrame.new(0, 0, 1.2)
                                    knife:Activate()
                                    task.wait(0.15)
                                end
                            end
                        end
                    end
                end)
                task.wait(0.2)
            end
        end)
    end
end)

-- ==================== 3. ШЕРИФ ====================
local TabSheriff = CreateTab("🔫 Шериф")

CreateToggle(TabSheriff, "Автоподбор оружия", function(v)
    _G.AutoPickupGun = v
    task.spawn(function()
        while _G.AutoPickupGun do
            pcall(function()
                for _, obj in pairs(Workspace:GetChildren()) do
                    if (obj.Name == "GunDrop" or obj.Name == "Gun") and obj:IsA("BasePart") and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
                        LocalPlayer.Character.HumanoidRootPart.CFrame = obj.CFrame
                    end
                end
            end)
            task.wait(0.4)
        end
    end)
end)

local SheriffFillingBtn = Instance.new("TextButton", TabSheriff)
SheriffFillingBtn.Size = UDim2.new(1, -6, 0, 34)
SheriffFillingBtn.BackgroundColor3 = Color3.fromRGB(60, 150, 255)
SheriffFillingBtn.Text = "  ⚡ Прокачанный Филлинг Шерифа"
SheriffFillingBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
SheriffFillingBtn.TextSize = 11
SheriffFillingBtn.Font = Enum.Font.GothamBold
Instance.new("UICorner", SheriffFillingBtn).CornerRadius = UDim.new(0, 6)

SheriffFillingBtn.MouseButton1Click:Connect(function()
    pcall(function()
        local found = false
        for _, obj in pairs(Workspace:GetDescendants()) do
            if obj.Name == "GunDrop" or obj.Name == "Gun" then
                if obj:IsA("BasePart") and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
                    obj.CFrame = LocalPlayer.Character.HumanoidRootPart.CFrame
                    found = true
                end
            end
        end
    end)
end)

local DistanceLabel = Instance.new("TextLabel", TabSheriff)
DistanceLabel.Size = UDim2.new(1, -6, 0, 34)
DistanceLabel.BackgroundColor3 = Color3.fromRGB(26, 26, 34)
DistanceLabel.Text = "  Дистанция до киллера: Поиск..."
DistanceLabel.TextColor3 = Color3.fromRGB(230, 230, 240)
DistanceLabel.TextSize = 11
DistanceLabel.Font = Enum.Font.GothamMedium
DistanceLabel.TextXAlignment = Enum.TextXAlignment.Left
Instance.new("UICorner", DistanceLabel).CornerRadius = UDim.new(0, 6)

task.spawn(function()
    while true do
        pcall(function()
            local killerFound = false
            for _, p in pairs(Players:GetPlayers()) do
                if p ~= LocalPlayer and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
                    if p.Backpack:FindFirstChild("Knife") or p.Character:FindFirstChild("Knife") then
                        killerFound = true
                        if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
                            local dist = (LocalPlayer.Character.HumanoidRootPart.Position - p.Character.HumanoidRootPart.Position).Magnitude
                            DistanceLabel.Text = string.format("  Дистанция до киллера: %.1f м", dist)
                        end
                    end
                end
            end
            if not killerFound then DistanceLabel.Text = "  Дистанция до киллера: Не найден" end
        end)
        task.wait(0.5)
    end
end)

CreateToggle(TabSheriff, "Аим бот (Шериф) [Зациклено]", function(v)
    _G.SheriffAimbot = v
    task.spawn(function()
        while _G.SheriffAimbot do
            pcall(function()
                for _, p in pairs(Players:GetPlayers()) do
                    if p ~= LocalPlayer and p.Character and p.Character:FindFirstChild("Head") then
                        if p.Backpack:FindFirstChild("Knife") or p.Character:FindFirstChild("Knife") then
                            Camera.CFrame = CFrame.new(Camera.CFrame.Position, p.Character.Head.Position)
                        end
                    end
                end
            end)
            task.wait()
        end
    end)
end)

CreateToggle(TabSheriff, "Авто стрельба [Зациклено]", function(v)
    _G.AutoShoot = v
    task.spawn(function()
        while _G.AutoShoot do
            pcall(function()
                local char = LocalPlayer.Character
                local gun = char and char:FindFirstChild("Gun") or LocalPlayer.Backpack:FindFirstChild("Gun")
                if gun then
                    if gun.Parent == LocalPlayer.Backpack then gun.Parent = char end
                    for _, p in pairs(Players:GetPlayers()) do
                        if p ~= LocalPlayer and p.Character and (p.Backpack:FindFirstChild("Knife") or p.Character:FindFirstChild("Knife")) then
                            gun:Activate()
                        end
                    end
                end
            end)
            task.wait(0.2)
        end
    end)
end)

-- ==================== 4. КНОПКА SHOOT ====================
local TabButton = CreateTab("🔘 Кнопка Shoot")

local ScreenShootBtn = Instance.new("TextButton", ScreenGui)
ScreenShootBtn.Size = UDim2.new(0, 100, 0, 45)
ScreenShootBtn.Position = UDim2.new(0.82, -50, 0.65, -22)
ScreenShootBtn.BackgroundColor3 = Color3.fromRGB(220, 50, 50)
ScreenShootBtn.Text = "SHOOT"
ScreenShootBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
ScreenShootBtn.TextSize = 14
ScreenShootBtn.Font = Enum.Font.GothamBold
ScreenShootBtn.Visible = false
ScreenShootBtn.Active = true
ScreenShootBtn.Draggable = true
Instance.new("UICorner", ScreenShootBtn).CornerRadius = UDim.new(0, 10)

ScreenShootBtn.MouseButton1Down:Connect(function()
    pcall(function()
        local char = LocalPlayer.Character
        local gun = char and char:FindFirstChild("Gun") or LocalPlayer.Backpack:FindFirstChild("Gun")
        if gun then
            if gun.Parent == LocalPlayer.Backpack then gun.Parent = char end
            gun:Activate()
        end
    end)
end)

CreateToggle(TabButton, "Показать экранную кнопку SHOOT", function(v)
    ScreenShootBtn.Visible = v
end)

-- ==================== 5. ДРУГОЕ И РАСТЯГ ====================
local TabOther = CreateTab("🎨 Другое")

local function AddColorBtn(name, color)
    local btn = Instance.new("TextButton", TabOther)
    btn.Size = UDim2.new(1, -6, 0, 32)
    btn.BackgroundColor3 = color
    btn.Text = "  Цвет: " .. name
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.TextSize = 11
    btn.Font = Enum.Font.GothamBold
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)
    btn.MouseButton1Click:Connect(function()
        _G.isRGB = false
        UpdateThemeColor(color)
    end)
end

AddColorBtn("Красный (Основной)", Color3.fromRGB(240, 60, 60))
AddColorBtn("Зеленый", Color3.fromRGB(50, 215, 110))
AddColorBtn("Синий", Color3.fromRGB(60, 150, 255))
AddColorBtn("Фиолетовый", Color3.fromRGB(175, 70, 255))

CreateToggle(TabOther, "RGB Цвет (Радужный интерфейс)", function(v)
    _G.isRGB = v
end)

local TabStretch = CreateTab("📐 Растяг")
local StretchToggleBtn = Instance.new("TextButton", TabStretch)
StretchToggleBtn.Size = UDim2.new(1, -6, 0, 34)
StretchToggleBtn.BackgroundColor3 = Color3.fromRGB(26, 26, 34)
StretchToggleBtn.Text = "  ⚡ Включить/Выключить растяг экрана"
StretchToggleBtn.TextColor3 = Color3.fromRGB(230, 230, 240)
StretchToggleBtn.TextSize = 11
StretchToggleBtn.Font = Enum.Font.GothamMedium
Instance.new("UICorner", StretchToggleBtn).CornerRadius = UDim.new(0, 6)

local stretchState = false
StretchToggleBtn.MouseButton1Click:Connect(function()
    stretchState = not stretchState
    if stretchState then
        Camera.FieldOfView = 95
        StretchToggleBtn.BackgroundColor3 = _G.CurrentAccent
        StretchToggleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    else
        Camera.FieldOfView = 70
        StretchToggleBtn.BackgroundColor3 = Color3.fromRGB(26, 26, 34)
        StretchToggleBtn.TextColor3 = Color3.fromRGB(230, 230, 240)
    end
end)
